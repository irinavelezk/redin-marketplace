[build]
builder = "NIXPACKS"

[deploy]
# Baileys auth must persist. Mount a Railway volume to /data.
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
TONO_DATA_DIR = "/data"
