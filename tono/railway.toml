[build]
builder = "DOCKERFILE"
dockerfilePath = "tono/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[deploy.volumes]]
name = "tono-baileys-auth"
mountPath = "/data"

[env]
TONO_DATA_DIR = "/data"
